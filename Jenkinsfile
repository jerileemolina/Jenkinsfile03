pipeline {
    agent any 
    stages {
        stage ('Ejercicio3') {
            steps {
                sh '''
                for variable in ('cat release.yml')
                do
                variable={string: -1}
                echo $variable
                done
                '''

                }
            }
        }
    }